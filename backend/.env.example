NODE_ENV=development
PORT=3037
API_PREFIX=/api
CORS_ORIGIN=http://localhost:8087
# LAN dev (HTTPS frontend): CORS_ORIGIN=https://192.168.x.x:8087

# Activa logs SQL de Prisma (incluye SELECT) solo para depuración puntual
PRISMA_LOG_QUERIES=false

# Master DB — registro de tenants (subdominios + credenciales BD)
MASTER_DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/cortexcc_master

# BD del tenant local (scripts migrate-tenant / seed; no usada en runtime del API)
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/cortexcontact

# Variables CLI para migrate-tenant (alternativa a DATABASE_URL)
# TENANT_DB_HOST=localhost
# TENANT_DB_PORT=5432
# TENANT_DB_USER=USER
# TENANT_DB_PASSWORD=PASSWORD
# TENANT_DB_NAME=cortexcontact

JWT_SECRET=change-me-min-32-characters-long-secret!!
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# JWT separado para el panel /platform (opcional; por defecto JWT_SECRET + "-platform-admin")
# PLATFORM_JWT_SECRET=change-me-platform-admin-min-32-chars-secret!!

# Bootstrap admin de plataforma (setup:platform-admin)
# PLATFORM_ADMIN_EMAIL=platform@cortex.local
# PLATFORM_ADMIN_PASSWORD=PlatformAdmin123!

# Redis en la máquina local (puerto por defecto 6379; el sufijo /2 es el número de base de datos lógica)
REDIS_URL=redis://localhost:6379/2
QUEUE_CONCURRENCY=5
ENABLE_JOBS=true

SOCKETIO_PATH=/socket.io
SOCKETIO_CORS_ORIGIN=http://localhost:8087
# LAN dev: igual que CORS_ORIGIN (https://192.168.x.x:8087)

# Clave compartida para integraciones externas en x-api-key (POST .../integrations/escalate)
INTEGRATION_API_KEY=dev-integration-api-key-change-me

# Ventana (en días) para la tasa de conversión del ruteo PRIORITY_BASED.
# 0 = usar el histórico acumulado de por vida (sales_won/sales_total).
PRIORITY_WINDOW_DAYS=30

STORAGE_PROVIDER=local
# STORAGE_LOCAL_DIR=uploads
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=attachments
